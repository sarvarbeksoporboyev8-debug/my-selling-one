name: dw_domain
description: Domain layer for DontWaste app - entities, repositories, usecases
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dw_api:
    path: ../dw_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
